/*
 * ${className}
 * Created on : ${date}
 * Author : ${username}
 */
${packageName}

${importStatements}

/**
 * TODO
 * 
 * @author ${username}
 */
object ${className} ${extends} ${traits} {

  def main(args: Array[String]) = {
  }
}